# ── SDK (download / extract separately — see README / setup.ps1) ─────────────
SDK_pynaoqi/linux64/
SDK_pynaoqi/pynaoqi/
SDK_pynaoqi/pynaoqi-python2.7-*-linux64*/
SDK_pynaoqi/*.tar.gz
SDK_pynaoqi/*.zip

# ── Model: model.bin ใหญ่ 782 MB → ต้อง download แยก ────────────────────────
# model/thonburian-large-ct2/model.bin
# model/thonburian-medium-ct2/
# model/model/
model/*

# ── Config (copy from config.example.json and fill in your API key) ──────────
config.json
config/config.json
backup/config_en.json

# ── Runtime files (auto-generated) ───────────────────────────────────────────
speech.mp3
command.txt
status.txt
query.txt
face_greeting_event.json
face_greeting_event.json.tmp
config/face_greeting_state.json
config/face_greeting_state.json.tmp

# ── Old / dev files ───────────────────────────────────────────────────────────
face_captures/
recordings/
bin/archive/

# ── Local venv ────────────────────────────────────────────────────────────────
.venv/
.venv-py2/
venv/

# ── Python cache ──────────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
# Keep NAOqi native module discoverable if someone force-adds the SDK tree
*.pyd
!SDK_pynaoqi/**/*.pyd
