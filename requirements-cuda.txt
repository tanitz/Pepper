# Optional CUDA runtime libs for Whisper CT2 (Windows/Linux + NVIDIA GPU).
# Install with: make install-cuda
# On Windows these wheels provide cublas64_12.dll and cuDNN 9 DLLs.
# Safe to skip on macOS and CPU-only hosts.
nvidia-cublas-cu12
nvidia-cudnn-cu12
